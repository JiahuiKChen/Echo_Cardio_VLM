# ASA revalidation readiness — 2026-09-09

The completed C3 store is the imaging input authority for this analysis phase.
The historical pre-C3 configuration and gate documents retain their original
meaning; their pending reconstruction flags do not reopen C3. This dated record
separates the completed reconstruction from the remaining analysis prerequisites.

**Subsequent owner-relayed review:** the owner supplied all eight operational
interpretations and relayed a qualified echocardiographer's agreement. The
[versioned clinical amendment](revalidation_2026-09-09/clinical_adjudication_amendment_2026_09_09.md)
supersedes the earlier human-action queue below. The original blank form is
preserved as historical evidence; its blank fields no longer mean the clinical
decisions are missing. The new route retains unknown expert identity/signature/date
and independently checks source-unit eligibility, including the E-field conflict.

## Completed reconstruction and owner instruction

The existing completion receipt SHA-256 is
`48ab39b48fb95e8257ab07c076a587151eeb993dbe638ce7d6908d3be7168605`.
It binds producer `cda842d04cc18eb3669ad5377c31a6e955fc43cb`, scientific commit
`e1cdb674ada23bbc9f3a1ff77c33927bd324d3ed`, and plan SHA-256
`904d0ab65f003c1eb68adeee8c0b1dd786ec7a9ef4bb496b646b22cc7a540247`.
All 19 batches finalized: 4,530 selected subjects/studies, 335,984 source
objects, 184,570 clip vectors, and 4,525 study vectors. The five prespecified
no-cine studies account for the difference. The four scientific/runtime epochs,
original sixteen receipts, and 42 preservation artifacts were validated during
completion. The sealed historical partial remains unadopted. Analysis preparation
consumes that completion evidence without repeating the full preservation replay.

The owner's 2026-09-09 instruction explicitly authorizes implementation, focused
and maintained validation, ordinary commits/pushes, input preparation, train/validation
fitting, and fixed-test evaluation after the actual analysis prerequisites and
lock pass. The exact instruction SHA-256 is
`f8d3285a2615af4321a416f4850e078a9b5180708c43fe7ff2c87a58a5849d8d`.
This records the existing authorization; it is not a clinician signoff. The
separate versioned analysis authority binds this instruction to the final source,
SAP/config, cohort/split, clinical panel, dependency masks, environment, and input
checksums. The institutional direct-restricted-access receipt remains access
plumbing and does not substitute for scientific readiness or export approval.

## Readiness evidence

| Requirement | Evidence | Status | Remaining action |
|---|---|---|---|
| Selected C3 imaging authority | Completion receipt plus canonical artifact hashes and serialized input replay | Complete | Preserve the original producer and derived analysis bindings |
| Original selected cohort and subject split | Selected 3,171/679/680; imaging eligible 3,168/678/679; exact membership and ownership passed | Verified on final store | Retain these fixed splits |
| LVEF label identity | Exact case-sensitive raw `lvef`; numeric within-report median; selected subject/measurement join; ordered label fingerprints replayed | Verified independently of canonical mapping | Preserve the separate label authority and method/unit limitations |
| LVEF thresholds | `<40` primary; `<=40` and `<50` sensitivities; final exact-40 count 71/12/20 | Label-only reconciliation passed | Keep thresholds fixed; all three endpoints pass class support floors |
| Technical metadata | Current SCC audit, complete 188-row mapping, exact regenerated 67-row view, hash-bound technical decision receipt | Nine dispositions published | Apply bounded exclusions and retain the stated limitations |
| Echocardiographer decisions | Exact owner-relayed expert agreement and eight supplied interpretations; original packet preserved | Supplied through versioned review route | Publish/replay the separately bound review; no repeat direct-entry form required |
| Scored panel and dependency masks | Fixed finalization applies explicit measurement definitions, positive raw predictors, aliases/formulas/families and supported units | Awaiting concrete SCC authority publication | Retain 21 reviewed measurement targets, separate LVEF anchor and the valid E-velocity source; replay before lock |
| Exact common modality inputs | Final-store LVEF 2,833 (1,997/410/426); all 22 candidate/anchor targets × three split fingerprints replayed | Verified shared rows and labels | Bind the clinically finalized subset without modality-specific row removal |
| Implementation and environment | Commit `073d388`; local maintained suite 2,063 passed/1 skipped; SCC focused suite 86 passed; private environment/source receipts | Validated | Preserve tested source/environment bindings |
| SAP/config and analysis lock | Existing prescription and owner authorization; new clinical route and concrete panel/dependency finalizer | Unsealed at this source checkpoint | Refresh source/environment/validation bindings, replay all gates, then seal and execute |
| Non-LVEF native-unit margins | May explicitly remain unresolved | Does not independently block error/interval analysis | Suppress margin-based win/tie/loss claims for targets without a defensible margin |

The completed SCC input preparation and receipt bindings are recorded in the
[current input funnel](revalidation_2026-09-09/current_input_funnel.md). The owner,
environment, source, technical decisions and synthetic-validation receipts are
actually published; they are not proposed filenames. The original C3 checkout
remains at its completed producer commit. The separate analysis checkout consumed
only existing stores and metadata. No model fitting, test-performance access,
new source download or encoder execution occurred. The preparation helper has
finished, and no statistical analysis job has been submitted.

## Nine technical dispositions

The existing technical audit deliberately writes
`COMPLETE_PENDING_SCIENTIFIC_ADJUDICATION`. Its successful execution proves that
the evidence packet was prepared; it does not itself resolve the nine questions.
`audit_lvef_analysis_readiness.py` can replay the five input hashes and ten output
hashes before reuse. Technical decisions are recorded separately after inspecting
that evidence. Source code already establishes these bounded rules:

The 2026-09-09 SCC search found no newer prepared technical/clinician packet in
the scoped existing audit roots. The maintained audit and questionnaire builder
then both completed with exit status zero in the new analysis metadata directory.
The complete mapping has 188 rows and 186 distinct raw names. All 186 training
raw names occur in the complete mapping; 119 lie outside the filtered clinical
view, and zero lie outside the complete map. The 67-row review view regenerated
exactly. There are zero ambiguous review raw names and 59 distinct technical
evidence fields. No validation or test values were used for these distributions.

Candidate-field/training-present counts, in the issue order below, are 5/5,
18/18, 4/4, 7/7, 7/7, 1/1, 2/2, 7/7, and 17/17. The EF candidate review has 15
prespecified pair-diagnostic rows. Presence establishes available review evidence,
not alias equivalence or a formula relationship. The separate LVEF authority
passed exact-name, median, selected pre-imaging, and exact-40 reconciliation.
Native source-unit metadata for the exact LVEF label remain unverified and actual
method mixture remains unknown. A subsequent hash-bound projection read only the
existing restricted evidence and training-completeness CSVs; it did not repeat the
source audit. The following processing decisions close the technical review with
bounded exclusions and limitations. They do not supply any of the eight clinical
answers or declare an alias merge clinically valid.

| Technical question | Evidence and permissible conclusion | Limitation retained |
|---|---|---|
| `BSA_FORMULA_WEIGHT_AVAILABILITY` | **Conservative exclusion.** Five training-present candidates include area, length, mass and two unrecognized numeric unit classes. Keep anthropometrics outside the primary echo macro, and uncertain formula shortcuts outside permitted predictors. | Do not assert a BSA formula or weight source from names alone; BSA is an ancestor of indexed mass, not unindexed LV mass. Any context use requires explicit supported units and reviewed identity. |
| `DIMENSION_CM_MM_UNITS` | **Resolved computational units.** Sixteen fields have explicit cm numeric measurements: convert by exactly 10 to mm. Two unknown-unit categorical fields have zero numeric values and supply no numeric dimension labels. Reject incompatible/unknown units before aggregation and support checks. | Unit conversion does not settle clinical definition, convention or alias merging. |
| `LVEDV_LVESV_FIELDS` | **Conservative exclusion.** Four numeric candidate volume exports are prohibited LVEF predictors, regardless of whether the projection's finite unit vocabulary recognizes their recorded tokens. | Deterministic EF equivalence requires compatible method/beat and shared target provenance; exclusion does not assert equivalence. An unrecognized projection token alone is not proof of an invalid source unit. |
| `LVEF_ALIASES` | **Conservative exclusion.** Preserve exact raw `lvef` as the sole label authority and prohibit the other candidate EF exports as predictors. Seven EF-related candidates and fifteen pair diagnostics were reviewed. | No candidate alias becomes a label authority, and numerical agreement or correlation does not establish identity. |
| `LVEF_METHOD_MIXTURE` | **Operational definition with limitation.** Retain the exact-name numeric-median label and historical analytical EF-percentage-point scale. Method-specific candidate exports do not identify the method of each exact-name label. Record method unspecified/unstratifiable. | Actual method mixture is unknown. Neither homogeneous methods nor a demonstrated mixed-method label is claimed; the retained export's limits are not an unbounded external research gate. |
| `LV_MASS_RWT_FIELDS` | **Conservative exclusion.** The one candidate field has 135 categorical training values and zero numeric values. Prohibit unadjudicated category coding and do not create a numeric mass/RWT label or formula edge. | Mass, RWT and indexed mass remain separate possible dependency sets; broader geometry is not an algebraic dependency. |
| `MITRAL_EA_EEPRIME_RATIO_FIELDS` | **Conservative exclusion.** Two ratio candidates have numeric training values and unknown declared units. Exclude these shortcut candidates; do not guess a unitless conversion or matched component identity. | The relation between the two E-labelled fields remains the separate clinician question. |
| `VELOCITY_MPS_CMPS_UNITS` | **Resolved computational units with one incompatible field.** Six candidate velocities have explicit m/s numeric values: convert by exactly 100 to cm/s. `mitral_e_velocity` instead has ms values, so its rows cannot supply a velocity label. It is outside candidate21. | Time is not converted to velocity. No merge with `mv_peak_e` is permitted without separately corrected source-unit authority, even if a clinician describes the same underlying construct. |
| `WALL_MOTION_FIELDS` | **Conservative exclusion.** All seventeen candidate fields are categorical with zero numeric values. Prohibit these unadjudicated exports as primary predictors; do not invent numeric scores or category mappings. | The technical review does not establish segmental/global/formula equivalence. Conservative exclusion permits proceeding without inventing that distinction. |

The historical panel builder's lexical fallback for an ambiguous raw-to-canonical
mapping is not permissible in the new input authority. Its unit tables and median
aggregation document implementation lineage, not clinical adjudication.

`prepare_lvef_revalidation_review.py` records the nine technical dispositions only
after replaying all ten packet outputs, checking the five source checksums, and
matching the reviewed technical manifest SHA-256
`4e6c7ab3e957cfb5a369739021e2c71a41400a35103e7a77aa4bce2080e7b22a`. A
different valid prepared packet cannot inherit these fixed rulings. It
also prepares a restricted grouped panel/dependency draft from the actual input
receipt, complete mapping, existing registries and clinical packet. The draft
materializes exact raw-field masks, source aggregation fields, and candidate
context grouped by measurement family. Uncertain fields remain excluded; an
absent registry edge never becomes proof of independence. Draft positive groups
retain their evidence basis and `UNREVIEWED` status, with final approval false.
The artifact supports a grouped review and does not create a new questionnaire
for each of the historical 870 provisional edges or fill any clinical answer.

## Original questionnaire and subsequent review route

The maintained questionnaire covers `ARCH_DIAM_LEVEL`,
`ASCENDING_AORTA_CONVENTION`, `INF_LAT_THICKNESS_DEFINITION`,
`IVC_DIAM_CONTEXT`, `LA_DIMEN_PLANE`, `MITRAL_E_FIELD_RELATIONSHIP`,
`SINUS_DIAM_CONVENTION`, and `TR_MMHG_DEFINITION`.

The original direct-entry workflow asked a qualified echo-measurement expert to choose the permitted
option for each question and supply the required rationale, expertise attestation,
and date. `UNRESOLVED_EXCLUDE` is a legitimate human decision, with no alias merge
and exclusion from scored panels. An agent must not fill it in on the clinician's
behalf. The actual newly prepared packet has SHA-256
`e5c6230c0f2a6f2b4f74d5ec5212b4e87f75b1350d4412b04bb4a7b8c82a71e7`.
Its bytes regenerated under the maintained producer's CSV parsing, and the
response's packet checksum passed. All eight choices/rationales and reviewer
fields were blank at the independent-preparation checkpoint, so the direct-entry
validator correctly reported incomplete signoff then. They remain unmodified.
The subsequent owner communication now supplies the genuine review through a
separate fixed owner-relayed route; no expert name, signature or review date is
invented, and no second entry of the same eight decisions is required.
For Q6, the concrete technical finding is that `mitral_e_velocity` carries a time
unit while the six candidate velocity measurements carry velocity units. The
existing questionnaire already displays the restricted source metadata. This
finding constrains processing; it does not select the clinician's answer or
change the hash-bound questionnaire.

The restricted files are `clinical_metadata_clinician_signoff_restricted.md`,
`clinical_metadata_clinician_response_restricted.json`, and
`clinical_metadata_clinician_packet_manifest_restricted.json` under the chosen
metadata run's `restricted/clinician_signoff/` directory. Reuse requires the
manifest's packet hash, exact regeneration from the authorized metadata/source
commit, and the maintained response validator. All metadata and reviewer details
remain restricted; the readiness projection contains only question IDs, allowed
decisions, statuses, counts, and hashes.

## Input audit contract

The denominator funnel is selected cohort → imaging eligible → observed target →
valid units/aggregation → exact common rows across modalities → declared
exclusions. Repeated exact-name LVEF measurements are aggregated by the numeric
median within `(subject_id, measurement_id)` before the selected-study join.
Other targets require their reviewed alias/unit rules and declared repeated-row
aggregation. Neither 4,530 selected studies nor 4,525 study vectors is the LVEF
label denominator. The final-store audit confirms common LVEF counts of 2,833 overall and
1,997/410/426 by split; 103 overall and 71/12/20 by split equal exactly 40.

All five no-cine studies are excluded from every primary modality. A row whose
allowed structured predictors are all missing remains eligible and uses training
imputation; the separately labeled structured full-availability sensitivity has a
different estimand. Prohibited predictors are removed before feature eligibility,
imputation, scaling, or missingness indicators. Exact ordered subject-study-target
rows and labels must match across vision, structured, and fusion inputs.

A scored target requires at least 120/40/40 train/validation/test observed common
labels, at least 250 total, and finite positive training-target IQR. Binary tasks
require at least 20 events and 20 nonevents in each split. Membership is fixed
before fitting; LVEF is a separate anchor outside the macro. The candidate21 list
is not a final panel, legacy29 is a historical sensitivity, and body-size/vital
fields are not primary echo-measurement macro targets. Historical counts may
change only through documented source lineage or declared exclusions.
