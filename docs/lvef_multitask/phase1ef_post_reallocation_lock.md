# Phase 1E-F post-reallocation pretransfer lock

Status: **attempts 001 and 002 are preserved after separate fail-closed
capacity-parser defects; attempt 003 is prepared offline but remains unexecuted
and unauthorized**.

Phase 1E-F is limited to read-only capacity capture, a bounded owner-private
control-authority backup and isolated restore test, and offline reconstruction
of the production authority packet. It grants no Google Cloud, scheduler,
DICOM-body, extraction, EchoPrime, embedding, modeling, prediction, or
confirmatory-performance scope.

## Frozen scientific and planning authority

The selected-only prospective C3 contract remains 4,530 studies and subjects,
335,984 exact public-source objects, and 1,216,569,133,322 source bytes. Zero
selected objects are missing, unexpected, inaccessible, ownership-conflicting,
or locator-conflicting. Historical object identity remains unestablished
because no historical object comparator exists.

The owner-accepted requester-pays estimates remain $136.101850,
$142.906680, and $171.488015 (low/base/high). Phase 1E-F does not revisit
pricing, billing, trial credits, Autoclass, source metadata, or SCC storage
costs.

Capacity gates retain the frozen projected peak of 1,611,642,076,332 bytes,
the 200,000,000,000-byte free-space reserve, and the 1,811,642,076,332-byte
minimum effective research quota. A live receipt must use the native SCC quota
authority's exact integer-KiB records rather than interpreting its rounded
`GB` display as decimal bytes. This corrects the evidence method without
changing any frozen threshold.

The immutable predecessor set comprises the six job-7104307 aggregates, six
Autoclass attempt-002 aggregates, Phase 1E-E capacity attempts 001/002, and
production attempt 005. Phase 1E-F checks their exact filenames, byte sizes,
SHA-256 values, and closed-schema/subordinate validation receipts only. It
does not repeat source reconciliation, object listing, Autoclass, pricing,
storage inventory, or production work.

The owner's administrative 50/950/1,000 composition is recorded separately
from machine capacity. Native expected quotas are 52,428,800 KiB backed and
2,044,723,200 KiB research, or 53,687,091,200 and 2,093,796,556,800 exact
bytes. If confirmed, the exact research margins are 282,154,480,468 bytes
above the minimum, 482,154,480,468 bytes after the projected peak, and
282,154,480,468 bytes beyond the required reserve. The commercial 1,950-GB
planning label remains owner-provided context; it is not substituted for the
native allocation record.

The exact native quota rows, `df -B1`, and mount evidence are the primary
capacity authorities. The root-controlled `pquota` implementation reads the
current native quota record when no dated snapshot argument is supplied and is
bound by its own size and SHA-256. Reconciliation requires the corresponding
secure mount target, mount-source fileset basename, root filesystem subroot,
and distinct device identity to agree; both secure roots must also be regular,
nonsymlink, non-bind mount authorities.

The human-facing `pquota` table is a secondary cross-check only. The live
diagnostic found exactly one backed and one research project row, both using
`RPROJECT`-prefix first-field classes; the owner-context `PROJECT`-prefix
matches were zero. The repaired parser accepts either exact alias while
rejecting duplicates, near matches, and contradictions. Its two-line wrapped
header, variable ASCII spacing, and two
indented subordinate user rows were identical after normalization between
redirected nonterminal and PTY captures. A parsed display that agrees with the
native authority passes; an unavailable or unparseable display is recorded as
nonblocking when native, filesystem, and mount authorities are complete; a
material parsed contradiction remains blocking.

Gate reporting uses the closed states `PASS`, `FAIL`, and `NOT_EVALUATED`.
`FAIL` means the required inputs were evaluated and did not pass;
`NOT_EVALUATED` records an upstream tooling or evidence failure without
misstating it as a negative capacity result.

Snapshots are **not independently enumerated** by Phase 1E-F. The capacity
calculation avoids snapshot double counting by using the current effective
project quota and current `df` physical availability exactly once; it neither
adds snapshot bytes nor claims that snapshots are absent. Thus the receipt can
support capacity arithmetic without serving as a snapshot-inventory or
retention-policy authority.

## No-clobber attempt history

The sole authorized live attempt,
`lvef_multitask_phase1ef_post_reallocation_lock_attempt_001`, stopped at
`CAPACITY_CAPTURE` before producing a capacity receipt. It is preserved under
governing commit `555bc5a4586ca2fe17b5d95deac91a377f9d362d`; neither its
research-tier root nor backed-tier root may be reused or repaired in place.
The failure was a parser-column defect: the synthetic native-quota fixture
treated the second native field as the project principal. The root-controlled
SCC `pquota` implementation documents that field as the fileset, while the
project authority is the first-field name. Read-only diagnosis found exactly
one expected row for each project authority, both using the native `root`
fileset scope. No capacity conclusion is drawn from that diagnosis because the
closed receipt did not complete.

Attempt 002,
`lvef_multitask_phase1ef_post_reallocation_lock_attempt_002`, ran exactly once
under governing commit `498e0b676ca08e91321743bf856676eb285dae38` and
stopped at `CAPACITY_CAPTURE` with the sanitized error
`PQUOTA_DISPLAY_ROWS_MISSING` and exit status 2. The parser required only the
owner-context `PROJECT`-prefix display rows, while the live table contained one exact backed
and one exact research `RPROJECT`-prefix project row. No downstream stage
began, no capacity receipt was sealed, and neither its research-tier nor
backed-tier root may be reused or repaired in place.

The repaired but unauthorized next attempt is
`lvef_multitask_phase1ef_post_reallocation_lock_attempt_003`. Its future
restricted receipt set will bind the exact governing commit, both project
quotas and file quotas, filesystem identities and physical availability, mount
reconciliation, the approved backup manifest, isolated restore receipt, prior
immutable aggregate hashes, the current production packet, zero-scope launch
envelope, and an exact unexecuted first-batch command. The final authority chain
is not disaster-recoverable merely because its hashes are recorded under the
research tier; a terminal no-clobber backed-tier recovery seal must preserve
the exact current environment, packet, launch envelope, command, and final
aggregate, or the current-commit packet gate remains failed.

Any next attempt must preserve all earlier capacity and production attempts. It may
not overwrite or repair them. Raw paths, filesystem sources, principal names,
Git descriptors, selected-manifest contents, and restore details remain under
owner-private SCC paths. Only closed, identifier-free aggregate summaries are
eligible for reviewed export.

Every substantial production destination is fail-closed to
`/restricted/projectnb`: raw and partial downloads, retries/quarantine, DICOM
audit, extracted cache, embeddings, state/resume ledgers, preservation,
scheduler stdout/stderr, working directories, TMPDIR, XDG/Torch/Matplotlib/
Numba/pip/joblib caches, and finalization products. The backed tier is limited
to Git/worktree control, lightweight configuration, receipts, checksum
manifests, and the bounded recovery pack.

## Backup and recovery boundary

Tracked code is protected by the synchronized Git origin and an owner-private,
checksum-bound bundle used for the isolated restore test. The bounded backup
allowlist may include only explicitly approved control authorities such as the
selected-study/source/split manifests, enriched source-metadata receipt,
pinned checkpoint, **current-commit** runtime-environment receipt, and frozen
aggregate evidence. The current environment must be captured before the
primary backup/restore witness; backing only a predecessor environment is
insufficient. Every copied file has a safe backup-relative name, byte size, and
SHA-256.

OAuth/ADC state, access or refresh tokens, Cloud SDK credential databases,
private requester-pays/project/billing values, session or preflight environment
files, unrestricted logs, DICOM bodies, extracted images, embeddings,
predictions, and unapproved patient-level data are excluded. The approved
selected/source/split and source-metadata control manifests may themselves
contain restricted identifiers or locators and remain owner-private; their
presence means the safety claim is absence of **disallowed bulk scientific
payloads**, not literal absence of every restricted scientific datum.
Interactive authentication remains owner-recreatable and is not a backup
payload.

The restore test uses a new isolated owner-private root. It recreates the exact
Git commit and linked-worktree authority, verifies committed script/config
hashes, restores the allowlisted non-Git controls, checks manifest equality,
permissions, regular-file/no-symlink invariants, and credential absence, and
does not modify the live common Git directory or either live worktree.

The exact live EchoPrime executable, packages, and environment-receipt hashes
are validated for the canary, but the environment is classified
`CHECKSUM_ONLY_NO_COPY_REQUIRED`, not externally reconstructable. The backup
does not copy the virtual environment, and deterministic reacquisition after
loss is not established. If the live environment is lost or changes, a fresh
environment rebuild and equivalence authority is required before inference;
the present lock must not be cited as proof of deterministic environment
recovery.

## Production lock

The production packet retains exactly 38 authority roles and 17 semantic
validations. The compatibility role `post_expansion_capacity_summary` is
strengthened to bind the closed Phase 1E-F capacity-plus-recovery aggregate;
it is not a historical Phase 1E-E receipt. The single semantic capacity gate
therefore verifies capacity, backed-control safety, backup, restore,
credential exclusion, and production-path binding without adding a 39th role
or an 18th gate.

The exact first-batch command is an owner-private, mode-600 packet artifact.
It is marked `UNEXECUTED`, names the predetermined launch-envelope and future
authorization-receipt locations, and grants zero scopes. The launch envelope
is built last to bind the execution environment, Phase 1E-F pretransfer lock,
and 38-role packet without a checksum cycle.

The dependency order is fixed: capacity receipt; current-commit environment
receipt; primary backup plus isolated restore; 19-batch control plane; exact
unexecuted first-batch command; Phase 1E-F composite; 38-role/17-gate packet;
launch envelope; final aggregate; and terminal backed-tier recovery seal last.
The terminal seal is deliberately downstream so it can bind the exact
timestamped final artifacts without a checksum cycle. A changed commit,
config, checkpoint, manifest, or environment creates a new attempt rather than
mutating any predecessor.

## Decision rule

`GO_PENDING_EXPLICIT_OWNER_AUTHORIZATION` is permitted only when the fresh
research byte/file, physical-filesystem, 200-GB reserve, backed-control
byte/file, mount reconciliation, control-write binding, backup, restore,
credential-exclusion, 38-role, 17-gate, current-commit packet, and zero-scope
launch-envelope checks all pass, and the exact final chain is sealed for
recovery on the backed tier. That status still does not authorize a body
request. The first batch requires a later written authorization naming the
exact commit, receipts, recovery seals, packet, launch envelope, command,
checkpoint, environment, source batch, and preservation boundary.

Model fitting remains separately blocked by the unresolved technical metadata
adjudications, qualified echocardiographer signoff, final leakage registry and
panels, common-denominator lock, final statistical-plan/config checksum, and
separate modeling and confirmatory-access authorizations.
