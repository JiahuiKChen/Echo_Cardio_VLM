# Phase 1I-R8U-R7H same-attempt historical partial repair

R7H is a narrow control-plane repair for the original Tasks 17--19. The
consumed R7F Task 17 stopped before science because the R7D topology gate
counted the required same-attempt Batch-16 failed partial as active while also
requiring zero active caches. R7H does not change the immutable plan, cohort,
source objects, DICOM or extraction semantics, EchoPrime, embeddings,
technical dispositions, preservation, cache retirement, models, predictions,
or confirmatory analysis.

## Closed historical-evidence exception

Only the existing Batch-16 partial has the same-attempt historical-payload
exception to the R7H live-cache count. Its authority is the external failed-partial seal and the fixed R7
continuation/preservation-terminal chain, bound to scientific attempt
`lvef_c3_full_904d0ab65f003c1e_e1cdb674`, Batch `c3_batch_015`, 4,757 files,
259 directories, 8,583,119,701 bytes, and the exact metadata-projection hash.
Validation reads metadata only and must not add an internal summary to the
sealed tree.

The root remains immutable, non-adoptable, outside finalized active topology,
and unavailable as current scientific output. A canonical `clips` sibling,
seal drift, an unsafe entry, an active scheduler or process reference, any
unknown cache, or any additional active cache fails closed. Cross-attempt
terminal evidence and all ordinary cache rules retain their previous behavior.
The exact passing classification is
`PASS_R7H_SEALED_SAME_ATTEMPT_PARTIAL_EXCLUDED_FROM_ACTIVE_TOPOLOGY`.

## Retained extraction metadata

Cache retirement removes `dicom_extraction/clips`; the producer's audit,
extraction, disposition, and completion evidence remains in the parent.
Validated metadata and applicable transition or failure receipts are counted
separately from payload caches. Their roles must follow the applicable
producer contracts and existing historical authority. A clipless parent is
not sufficient evidence, and a filename suffix does not authorize content.
Unexpected children, hidden or nested payloads, symlinks, unsafe modes,
unbound partials, and invalid receipt bindings remain failures. Recognizing
a retained failure receipt as metadata never converts it into scientific
success or makes its partial adoptable.

The private diagnostic at `95b10584` found 16 finalized metadata roots in the
current attempt and three finalized legacy roots in an older attempt. The
first 15 current roots retain seven files, including technical dispositions
and two transition receipts. The current Batch-16 root retains five files
and has no transition-receipt directory. The three legacy roots retain six
files, including transitions but no technical-disposition manifest. The two
additional Batch-16 controls are the original R5 and R6 publication claims,
bound to their exact claim hashes and consumed-failure evidence hashes; they
are not generic failure directories. These counts describe the observed
history and do not define an acceptance whitelist.

The shared classifier is used by capacity, probe, every tail-task PREBODY,
and the finalizer wrapper. Retained metadata counts and occupied bytes are
informational and may grow after successful Tasks 17, 18, and 19; active-cache
and historical-partial protections remain fixed. Capacity continues to use
the actual filesystem and quota occupancy, including retained metadata.

## Authority and execution epoch

The no-clobber root is `r8u_r7h_continuation_17_19`. Its authorities
separate the unchanged scientific commit
`e1cdb674ada23bbc9f3a1ff77c33927bd324d3ed`, consumed R7F runtime commit
`2223d9768a1cc23efbe95a3c5474ea747a383a10`, R7G-R1 adjudication commit
`cf83c19521a2ed7b722c29a44c01f00cad0cf717`, the R7H base implementation
`8230d1535247256529616cb481dd48cce1f9a78f`, sealed-runtime correction
`95b105841fd1af69e3d29f3e1b4640de15ab25df`, and the topology correction
derived from the committed checkout. The permitted chain is exactly
R7G-R1 → R7H base → sealed-runtime correction → topology correction.
Each link has one parent and distance
one; merges and additional descendants are rejected. The branch, clean
tracked tree, origin/current-commit agreement where required, and scientific
ancestry remain enforced. The R7F and R7G roots remain read-only.

The base stopped at capacity runtime validation before capacity commands or
R7H control publication because it selected live capture for an established
sealed continuation. This is `CONTROL_PLANE_IMPLEMENTATION`, triggered by
`VOLATILE_INFRASTRUCTURE_METADATA`: kernel patch `553.153.1` → `553.158.1`.
The sealed-runtime correction selects
`SEALED_SCHEDULER_RUNTIME_REPLAY`, as do topology readback, worker submission
validation, and the sequential prebody, EchoPrime, and preservation wrappers.
The existing replay contract keeps the OS difference diagnostic while
requiring the sealed receipt SHA-256 before and after validation, the live
package inventory, and every portable runtime field. Live capture retains
its global default. The environment receipt is neither rewritten nor
resealed, and live scheduler, account/UID, job/task/role, runner/interpreter,
claim, and submission checks still validate the current invocation.

The sealed-runtime correction subsequently stopped before resource commands
because the topology classifier treated retained producer metadata as an
unknown extraction cache. When established by the retained receipt roles,
that defect is `CONTROL_PLANE_IMPLEMENTATION`, not a resource deficit or
scientific failure. The existing consumed-R7F evidence remains sealed under
its original producer commit and exact hash; fresh topology authority binds
that historical evidence to the current implementation without overwriting
the predecessor or repeating sealed accounting queries.
The consumed-evidence SHA-256 is
`40fb2c77f2eaebb67bbc34e085c23bd9ad83351593dcb67978e3a2da2ca7741d`;
its producer remains `95b105841fd1af69e3d29f3e1b4640de15ab25df`.
The existing topology-authority schema supplies the successor binding through
`r7h_runtime_commit` and `consumed_r7f_evidence_sha256`, without an additional
receipt or namespace. Missing or substituted source evidence fails with
`R7H_CONTROL_EPOCH_MISMATCH` or `R7H_CONTROL_EVIDENCE_HASH`.

Fresh controls, script hashes, claims, jobs, and tail receipts bind the
topology correction. Neither pre-scientific R7H implementation produced
scientific receipts, so the finalizer
continues to require exactly four scientific implementation epochs: original
Batches 1--2, the fixed Batches 3--15 epoch, the fixed R7 Batch-16 epoch, and
one current corrective epoch shared by Batches 17--19.

R7H binds all 16 finalized prefix receipt hashes, the exact Batch-16 final
receipt, the external failed-partial evidence, R7G terminal authority, all
four consumed-R7F accounting receipts, the new topology authority, the one
targeted capacity receipt, and the new scheduler-account authority. The
R7G terminal-authority SHA-256 is
`3764a284f3b8ba2a5bf3e9abbc9708a6dc2c9cecfa8e2bbef05a2b507252610e`.
The known Task-17 accounting receipt, SHA-256
`454c8204ff7b6fc6d23dac81efd56ea4464121634eb85ba5557790a6195fde7d`,
is reused without another qacct query; at most one fixed read-only query may
close each of Task 18, Task 19, and the old finalizer. A consumed R7F failure
receipt can never serve as a batch-final receipt.

## Probe and final submission

After the exact R7H capacity status
`PASS_R7H_TASKS_17_19_REMAINING_CAPACITY`, one CPU-only array-task-17 probe
uses the same worker entrypoint and topology validator as the scientific
array. It has one slot, no GPU, a ten-minute limit, no scientific job-storage
tree, and cannot reach cloud, DICOM/NPZ bodies, extraction, EchoPrime,
embedding, preservation, or scientific outputs. Its terminal status is
`PASS_R7H_CONTINUATION_TOPOLOGY_PROBE`.

Only that PASS permits one GPU array fixed to Tasks 17--19 with `-tc 1`, then
one CPU finalizer held on that array. Batch 19 remains the 30-study terminal
batch. The finalizer accepts the existing Batch 1--16 final receipts and only
the new R7H Batch 17--19 final receipts. The maximum new scheduler submissions
is three: one probe, one scientific array, and one held finalizer.
